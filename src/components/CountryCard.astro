---
interface Props {
  slug: string;
  name: string;
  flag?: string;
  summary?: string;
  imageUrl?: string;
}
const { slug, name, flag = '', summary = '', imageUrl = '' } = Astro.props;
---
<a
  href={`/destinations/${slug}`}
  class="tilt-card group relative block h-40 overflow-hidden rounded-2xl border border-brand-line [perspective:1000px]"
  data-country-name={name}
>
  <div class="tilt-inner absolute inset-0 flex items-center justify-center bg-gradient-brand text-5xl transition-transform duration-300" style={imageUrl ? `background-image:url(${imageUrl});background-size:cover;background-position:center;` : ''}>
    {!imageUrl && flag}
  </div>
  <div class="absolute inset-0 bg-gradient-to-t from-black/75 via-black/10 to-transparent"></div>
  <div class="absolute inset-x-0 bottom-0 p-4">
    <span class="block text-sm font-bold text-white">{flag} {name}</span>
    {summary && <span class="mt-0.5 block truncate text-xs text-white/70">{summary}</span>}
  </div>
</a>

<script>
  document.querySelectorAll<HTMLElement>('.tilt-card').forEach((card) => {
    const inner = card.querySelector<HTMLElement>('.tilt-inner');
    card.addEventListener('mousemove', (e) => {
      const r = card.getBoundingClientRect();
      const x = (e.clientX - r.left) / r.width - 0.5;
      const y = (e.clientY - r.top) / r.height - 0.5;
      card.style.transform = `translateY(-6px) rotateY(${x * 14}deg) rotateX(${-y * 10}deg)`;
      card.style.boxShadow = `${-x * 16}px ${-y * 8}px 32px rgba(11,20,36,0.18)`;
      if (inner) inner.style.transform = `scale(1.08)`;
    });
    card.addEventListener('mouseleave', () => {
      card.style.transform = '';
      card.style.boxShadow = '';
      if (inner) inner.style.transform = '';
    });
  });
</script>
